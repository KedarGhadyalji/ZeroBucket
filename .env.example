# Copy this file to .env (or just set these in your shell) and fill in real values.
# .env itself is gitignored -- never commit real credentials.

ZEROBUCKET_TEST_DATABASE_URL=postgresql://postgres:your_password@localhost:5432/zerobucket_test
